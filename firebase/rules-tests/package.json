{
  "name": "id-entity-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Security-rule tests for ID entity. Runs against the Firebase emulator - free, no Blaze plan, no network project needed.",
  "scripts": {
    "pretest": "bash free-ports.sh",
    "test": "firebase emulators:exec --config ../firebase.json --only firestore,storage --project id-entity-rules-test \"mocha --timeout 25000 firestore.test.js && mocha --timeout 25000 storage.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-tools": "^13.35.1",
    "mocha": "^10.8.2"
  }
}